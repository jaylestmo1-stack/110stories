## 2026-03-15 - Interactive Crisis UX & Semantic Navigation
**Learning:** Digital archives benefit from direct `tel:`/`sms:` URIs, semantic `<nav>` elements for quick navigation, and standard header levels (`##`) for reliable screen-reader and TOC indexing.
**Action:** Embed mobile-responsive call triggers, use semantic wrappers, and enforce strict visual hierarchy.

## 2026-03-17 - Keyboard-First Navigation with Accesskeys
**Learning:** Adding standard HTML `accesskey` attributes coupled with clear dual-platform `<kbd>` shortcut hints empowers power users and assistive technology users to jump instantly to critical document landmarks.
**Action:** Embed `accesskey` on primary navigation elements and document them clearly.
